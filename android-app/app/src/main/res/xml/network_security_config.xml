<?xml version="1.0" encoding="utf-8"?>
<!--
    Production network policy:
      - Cleartext blocked everywhere by default.
      - 10.0.2.2 (Android emulator loopback) gets a debug-only allowance so
        local development against http://localhost:8000 keeps working.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Allow cleartext only for the Android emulator's host loopback. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
